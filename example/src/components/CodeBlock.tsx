/**
 * CodeBlock Component - Main Export
 * 
 * Re-exports the main CodeBlock component for backward compatibility
 * while maintaining the new modular architecture
 */

export { default } from './CodeBlock/CodeBlock';
export type { CodeBlockProps } from './CodeBlock/types';

export default type